angular.module('oldApp').controller('HeaderNav', function ($scope,$state) {
    'use strict';
    	// function to submit the form after all validation has occurred			
	// $scope.menuNav = function(navOption) {
	// 	if (navOption === 'aboutUs') {         
    //         $state.go('aboutUs')
	// 	}else if(navOption === 'donor'){
    //         $state.go('donor')
    //     }

    // };
    $scope.la='la'
    
});

